# NN-功能名 · 发布部署

> 功能级发布快照，对应 `开发流程SOP.md` 阶段 8。
> **本文件是「本次上线」的快照**——记录这个功能上线那一次做了什么。后续小修不在此追加。
> 完整冷启动手册见 `docs/部署手册.md`；跨功能可复用坑写 `docs/踩坑记录.md`；过程笔记去灵计一动看板。
> 章节固定、按编号保留，不适用就注明「不适用」、不删。

---

## 1. 上线概览

- **上线日期**：YYYY-MM-DD
- **版本号**：vX.Y.Z（在此声明，阶段 9 抄进根 `CHANGELOG.md`）
- **影响面**：（前端 / 后端 / 数据 / 基础设施，选适用）
- **回滚预案**：（一句话：出问题怎么回到安全状态）

---

## 2. 改动汇总

### 2.1 代码改动
（新增 / 变更的模块、接口、页面）

### 2.2 数据变更 / 迁移
（如有：迁移脚本编号 + 内容；不改数据则写「不适用」）

### 2.3 配置 / 基础设施变更
（如有：路由、容器、定时任务、第三方服务等；没有则写「不适用」）

### 2.4 环境变量

| 变量名 | 默认值 | 说明 | 是否必填 |
|---|---|---|---|
|  |  |  |  |

> 新增 env 须**同步到所有相关位置**（配置示例、部署编排、本文件、`docs/部署手册.md`），漏一处线上就会起不来。

---

## 3. 发布步骤（本次上线）

> 按项目实际技术栈填具体命令。顺序执行，最后一步必做冒烟测试。

1. 准备：拉取新代码 / 更新配置（如有新 env）
2. 数据迁移（如有）
3. 构建并部署受影响的服务
4. **冒烟测试**：核心接口 / 页面跑通，确认无报错

---

## 4. 回滚步骤

> 出问题时按此快速回到上线前状态。这里写具体回滚**操作**；设计阶段定的回滚**边界/约束**见 `设计.md` §4.7。

1. 代码回滚：（回退到上一个稳定版本）
2. 数据回滚：（如改了 schema —— 通常不真删数据，回退代码即可；新增字段 / 表留着不影响旧逻辑）
3. 验证：回滚后再跑一遍冒烟测试
