# NN-功能名 · 设计

> 功能级设计文档，对应 `开发流程SOP.md` §四 阶段 1~3：**立项需求 → 技术方案 → UI 设计**。
> 复制 `docs/功能/_模板/` 后填写，把标题里的 `NN-功能名` 换成实际名。
> 各阶段产出后按闸门停下请用户评审（标 ✅ 的阶段）再进下一节。
> 计划、进度、过程笔记**不写这里** —— 去灵计一动看板。

---

## 1. 需求（SOP 阶段 1 立项）

### 1.1 做什么
（一句话说清这个功能是什么）

### 1.2 意图 / 想达成的效果
（这个功能想达成什么 —— 帮 AI 选对实现方向，轻量即可，不必论证立项）

### 1.3 不做什么（边界）
- 不做 A，因为 …
- 不做 B，因为 …

### 1.4 怎么算做好了
（能判断完成即可，不强求量化数字）

---

## 2. 技术方案（SOP 阶段 2）

> 含调研：动手定方案前先查清官方文档 / 竞品做法 / 项目内可复用代码，再据此设计。

### 2.1 调研
- **关键技术 / API 用法**：查官方文档得到的结论（用哪个 API、怎么用、有什么坑）
- **竞品 / 行业做法**：别人怎么解决，值得借鉴或避开什么
- **项目内可复用**：grep 出来的可复用模块 / 组件 / 接口，避免重复造轮子

### 2.2 总览
（架构图 / 数据流，mermaid 或 ASCII，看清楚数据从哪来到哪去）

### 2.3 数据模型
（实体 / 表：字段、约束、索引；如有数据迁移，记下迁移脚本编号）

### 2.4 接口契约

| 方法 | 路径 / 名称 | 入参 | 出参 | 说明 |
|---|---|---|---|---|
|  |  |  |  |  |

### 2.5 关键决策（ADR）
- **决策 1**：选 A 不选 B，因为 …
- **决策 2**：…

### 2.6 与既有功能的耦合点
- 依赖 X 功能（已在 `docs/功能/NN-X功能/设计.md` 定义，此处仅引用，不重复规格）
- 影响 Y 功能：需同步改 …

### 2.7 迁移 / 兼容策略
（如改动既有数据或接口：老数据怎么办、是否向后兼容、灰度策略）

### 2.8 风险与回滚边界
（设计阶段定的**约束**：本功能出问题最坏回滚到什么状态、哪些操作必须可逆。具体回滚**操作步骤**上线时写进 `发布部署.md` §4，此处只定边界、不写命令）

---

## 3. UI 设计（SOP 阶段 3）

> 无前端则本章填「本功能无前端，不适用」—— **保留章节**、不删（SOP 与跨文档引用都靠章节号定位）。

### 3.1 设计依据
- 沿用 `docs/设计规范.md`（§2 配色 / §3 字体 / §4 间距 / §5 组件）
- 本功能新增的 token / 组件：（若有，记得补回 `设计规范.md` 对应章节）

### 3.2 设计稿（线框图 / 视觉稿）
- **开工先按 `开发流程SOP.md` §四「UI 出图路线」选路线**（A 直接高保真 / B SVG 线稿先行 / C AI 生图作参考）；遵守逐屏铁律：一画面一稿、真实视口尺寸、禁止多页塞一张缩略图
- 路线 C 的 AI 生图**仅作参考**，必须再转出可开发的高保真稿才算完成
- `资源/` 下的线框图 / 参考图 / 视觉稿文件（写相对路径）

### 3.3 状态规划
（响应式 / 暗色模式 / 加载中 / 出错 / 空数据 分别长什么样）

### 3.4 文案规范
- 用户可见文案统一、符合产品调性
- 会进入 URL / 接口标识的字段用英文，避免编码问题
