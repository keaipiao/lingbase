# 部署手册

> 项目级冷启动手册——**从零到当前版本**，怎么把项目完整跑起来。
> living document：每次上线引入新环境变量 / 服务 / 步骤，都累积进来，让它**永远停在当前 HEAD**。
> 单次上线的快照在各功能 `发布部署.md`；这里是「从 0 到现在」的完整版。
> 章节固定、按编号保留，不适用就注明「不适用」、不删。

---

## 1. 前置准备

（需要的账号 / 服务器 / 域名，以及运行环境与版本，如 运行时 ≥ x、数据库 x、构建工具 x）

---

## 2. 获取代码与配置

1. clone 仓库
2. 复制配置样板并填真实值（如 `cp .env.example .env`）
3. dev / prod 用各自的配置区分（见 `架构总览.md` § 6）

---

## 3. 环境变量汇总

> 全项目环境变量的**权威清单**——各功能上线新增的 env 都累积到这里（与 `发布部署.md` 同步）。
> 遵守配置最小化：只列真正必要的（见 `CLAUDE.md`）。

| 变量 | 必填 | 默认 | 说明 |
|---|---|---|---|
|  |  |  |  |

---

## 4. 启动步骤（从零）

> 按项目实际技术栈填具体命令。

1. 安装依赖
2. 初始化数据存储（建库 / 跑迁移）
3. 构建
4. 启动服务

---

## 5. 验证

（冒烟：访问什么地址 / 跑什么命令，看到什么算成功）

---

## 6. 运维常用操作

- **更新部署**：拉新代码 → 重建 → 重启
- **看日志 / 排障**：（日志在哪、怎么看）
- **备份**：（数据怎么备）
- **回滚**：单次上线的回滚步骤见对应功能 `发布部署.md` § 4
